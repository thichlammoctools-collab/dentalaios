import type { MiddlewareHandler } from "hono"; import type { PlatformJwtPayload } from "@shared/types"; import { PLATFORM_MFA_STEP_UP_SECONDS, PLATFORM_SESSION_IDLE_SECONDS } from "@shared/constants"; import type { Env } from "../index"; import { verifyPlatformJwt } from "../lib/platform-jwt"; import { ForbiddenError, UnauthorizedError } from "../lib/errors"; import { createPlatformSessionsRepository } from "../repositories/platform-sessions.repo"; import { createPlatformUsersRepository } from "../repositories/platform-users.repo"; export type PlatformAuthContext = { platformJwt: PlatformJwtPayload; platformMfaVerifiedAt: string; }; const valid = (value: string, maxAge?: number): boolean => { const time = Date.parse(value); return Number.isFinite(time) && time > Date.now() - (maxAge ?? 0); }; export function requirePlatformAuth(): MiddlewareHandler<{ Bindings: Env; Variables: PlatformAuthContext }> { return async (c, next) => { const header = c.req.header("Authorization"); if (!header?.startsWith("Bearer ")) throw new UnauthorizedError("Missing or invalid Authorization header"); try { const jwt = await verifyPlatformJwt(header.slice(7).trim(), c.env.PLATFORM_JWT_SECRET); const session = await createPlatformSessionsRepository(c.env.DB).find(jwt.sid); if (!session || session.platform_user_id !== jwt.sub || session.revoked_at || !valid(session.expires_at) || !valid(session.last_seen_at, PLATFORM_SESSION_IDLE_SECONDS)) throw new UnauthorizedError("Platform session expired"); const user = await createPlatformUsersRepository(c.env.DB).findById(jwt.sub); if (!user || !user.user.is_active) throw new UnauthorizedError("Platform account is inactive"); if (Date.now() - Date.parse(session.last_seen_at) > 60_000) await createPlatformSessionsRepository(c.env.DB).touch(session.id); c.set("platformJwt", { ...jwt, role_key: user.role.key, permissions: user.role.permissions }); c.set("platformMfaVerifiedAt", session.mfa_verified_at); await next(); } catch (error) { if (error instanceof UnauthorizedError) throw error; throw new UnauthorizedError("Invalid or expired platform token"); } }; } export function getPlatformJwt(c: { get(key: "platformJwt"): PlatformJwtPayload | undefined }): PlatformJwtPayload { const jwt = c.get("platformJwt"); if (!jwt) throw new Error("Platform JWT not set"); return jwt; } export function requireRecentPlatformMfa(): MiddlewareHandler<{ Bindings: Env; Variables: PlatformAuthContext }> { return async (c, next) => { const at = c.get("platformMfaVerifiedAt"); if (!at || !valid(at, PLATFORM_MFA_STEP_UP_SECONDS)) throw new ForbiddenError("Recent MFA verification required"); await next(); }; }
