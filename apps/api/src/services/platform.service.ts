import type { D1Database } from "@cloudflare/workers-types"; import type { PlatformDashboardSnapshot, PlatformTenantDetail } from "@shared/types"; import { NotFoundError } from "../lib/errors"; import { createPlatformConfigRepository } from "../repositories/platform-config.repo"; import { createPlatformTenantsRepository } from "../repositories/platform-tenants.repo"; import type { D1Row } from "../repositories/base"; export const platformService = { async dashboard(db: D1Database, range: number): Promise<PlatformDashboardSnapshot> { const since = new Date(Date.now() - range * 86400000).toISOString(); const [tenants, users, branches, integrations] = await Promise.all([db.prepare("SELECT SUM(CASE WHEN is_active = 1 THEN 1 ELSE 0 END) active, SUM(CASE WHEN is_active = 0 THEN 1 ELSE 0 END) suspended, SUM(CASE WHEN datetime(created_at) >= datetime(?) THEN 1 ELSE 0 END) new_tenants FROM tenants").bind(since).first<D1Row>(), db.prepare("SELECT COUNT(*) count FROM users WHERE is_active = 1").bind().first<D1Row>(), db.prepare("SELECT COUNT(*) count FROM branches").bind().first<D1Row>(), db.prepare("SELECT COUNT(*) count FROM platform_integration_status WHERE health_status IN ('degraded', 'down')").bind().first<D1Row>()]); return { generated_at: new Date().toISOString(), active_tenants: Number(tenants?.active ?? 0), suspended_tenants: Number(tenants?.suspended ?? 0), new_tenants: Number(tenants?.new_tenants ?? 0), active_users: Number(users?.count ?? 0), branches: Number(branches?.count ?? 0), unhealthy_integrations: Number(integrations?.count ?? 0) }; }, async tenantDetail(db: D1Database, id: string): Promise<PlatformTenantDetail> { const tenant = await createPlatformTenantsRepository(db).get(id); if (!tenant) throw new NotFoundError("Tenant not found"); const config = createPlatformConfigRepository(db); const [limits, flags, integrations] = await Promise.all([config.limits(id), config.tenantFlags(id), config.integrations(id)]); return { ...tenant, ...(limits ? { limits } : {}), flags, integrations }; } };
